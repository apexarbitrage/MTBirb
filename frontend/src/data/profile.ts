/*
 * Shared sample profile data (the "You" tab's user), so anywhere else in the
 * app that needs the rider's name reads from one source instead of a second
 * hardcoded literal.
 */

export const PROFILE = {
  name: "Max Gokhman",
  firstName: "Max",
  homeLocation: "Redwood City, CA",
  memberSince: 2024,
};
